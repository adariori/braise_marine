Plat : id_plat, nom, description, prix, image_url, est_disponible

Categorie : id_categorie, libelle, icone

Reservation : id_reser, date_heure, nb_personnes, statut, commentaire

Client : id_client, nom_complet, telephone, email

Accompagnement : id_acc, nom, supplement_prix

Commande : id_com, date_heure, type, total_prix, adresse_livraison, statut

Livreur : id_livreur, nom, vehicule, telephone



CLIENT --(Effectuer)--> RESERVATION : Un client réserve une ou plusieurs fois.

PLAT --(Appartenir)--> CATEGORIE : Chaque plat est rangé dans une case (ex: le "Poulet Braisé" est dans "Grillades").









### Module 3 : Le Tunnel de Commande (La Passerelle)
*C'est ce qui fait le lien entre ton catalogue et ton encaissement.*

**Étape 1 : Le "Panier" invisible (LocalStorage)**
*   **Le but :** Ne plus simplement "afficher" une modal, mais transformer le clic sur "Commander" en une sauvegarde réelle.
*   **En JS :** Apprendre à utiliser `JSON.stringify()` pour transformer ton plat en texte et le stocker dans la mémoire du navigateur. Cela permet de ne pas perdre le choix du client s'il rafraîchit la page.

**Étape 2 : La Logique d'Accompagnement (Options)**
*   **Le but :** Dans ta modal, ajouter des "checkbox" ou "radio buttons" pour les accompagnements (Alloco, Riz, Attiéké).
*   **En JS :** Créer une fonction qui écoute le changement de ces options pour mettre à jour le prix total affiché dans la modal avant la validation.

---

### Module 4 : La Page de Caisse (`commande.html`)
*C'est ici que l'on prépare le terrain pour Stripe.*

**Étape 1 : Récupération et Rendu du Panier**
*   **Le but :** Sur cette nouvelle page, lire ce qui a été stocké dans le `localStorage` pour l'afficher fièrement au client ("Votre sélection : Poisson Braisé - 20€").
*   **En JS :** Utiliser `JSON.parse()` pour récupérer l'objet et reconstruire le résumé de la commande.

**Étape 2 : Le Formulaire de Livraison Intelligent**
*   **Le but :** Un formulaire qui s'adapte. Si le client choisit "Livraison", on affiche le champ "Adresse" ; s'il choisit "À emporter", on le cache.
*   **En JS :** Manipuler les classes `hidden` / `block` sur les champs d'adresse en fonction du type de commande choisi.

---

### Module 5 : Finalisation & Sécurité (Pré-Stripe)
*S'assurer que tout est parfait avant de parler d'argent.*

**Étape 1 : Validation stricte des données**
*   **Le but :** Vérifier que le numéro de téléphone est valide et que l'heure de retrait est cohérente avec les horaires d'ouverture du restaurant.
*   **En JS :** Utiliser des expressions régulières (Regex) pour le téléphone et des conditions sur l'objet `Date()`.

**Étape 2 : Le "Mock" de Paiement (Simulateur)**
*   **Le but :** Créer une transition fluide vers un écran de chargement puis vers une page de succès, en attendant d'intégrer les clés API de Stripe.

---

### Ce que ton fichier `main.js` va devenir (Nouvelle structure) :

```javascript
// --- 1. CONFIGURATION & ÉTAT (Ce qu'on garde) ---
const menuData = [...];
let commandeActuelle = { plat: null, options: [], total: 0 };

// --- 2. GESTION DU STOCKAGE (Nouveau !) ---
function sauvegarderChoix(plat) { ... } // localStorage.setItem
function recupererChoix() { ... }       // localStorage.getItem

// --- 3. MODAL & OPTIONS (Évolution) ---
function calculerPrixTotalModal() { ... } // Additionne plat + suppléments
function validerEtRediriger() { ... }    // Sauvegarde et window.location.href

// --- 4. FORMULAIRES & VALIDATION ---
function verifierHoraires() { ... }      // Empêche de commander à 3h du matin
```

### Prochaine action concrète :
Pour avancer, tu dois maintenant décider du **"carnet de notes"** de tes accompagnements. 

**Conseil :** Dans ton tableau `plats`, ajoute une propriété `accompagnements_possibles` (un tableau de noms). Cela te permettra de générer les cases à cocher dans ta modal de manière automatique !

**On commence par modifier tes données de plats pour y ajouter les accompagnements ou on s'attaque directement au code de sauvegarde ?**